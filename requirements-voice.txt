# Clyra voice worker: Pipecat + SmallWebRTCTransport + Fish Audio STT/TTS +
# DeepSeek (OpenAI-compatible) LLM. No local STT/TTS models -- this venv is
# intentionally light.
#
# numba/llvmlite/cryptography/numpy are pinned below because pipecat-ai's
# default resolution picks versions with no prebuilt wheel on Intel macOS,
# forcing a from-source build that needs a Rust toolchain + system LLVM/
# OpenSSL headers this machine doesn't have. These pins have wheels and are
# still within pipecat-ai's accepted ranges. Drop them on a machine that has
# a full build toolchain (or on Apple Silicon / Linux, which have wheels for
# the newer versions).
numba<0.62
llvmlite<0.45
cryptography<49
numpy<2.3

pipecat-ai[webrtc,silero,openai]>=0.0.108
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiohttp>=3.9.0
ormsgpack>=1.5.0
python-multipart>=0.0.9
